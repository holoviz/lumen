{% extends 'BaseLumenAgent/revise_output.jinja2' %}

{% block instructions %}
{{ super() }}

{% if discovery_context is defined and discovery_context %}
## Discovery Results
{{ discovery_context }}
{% endif %}

SQL-Specific Requirements:
- For "column not found" errors: Replace invalid column name with valid column from available schema
- For "table not found" errors: Use correct data table name from available tables
- For syntax errors: Fix only specific syntax issue while preserving query structure
- For DuckDB dialect: Use double quotes for column names, single quotes for string literals
- Preserve original query intent - don't completely rewrite unless structure is fundamentally broken
- Use exact column names from schema (case-sensitive)
{% endblock %}
