{% extends 'Agent/main.jinja2' %}

{%- block instructions %}
## Instructions
{%- if memory.get('sql') -%}
Analyze if the SQL query matches the user's request by checking:
1. What the user asked for
2. What the SQL actually does (read it carefully)
3. If they align

VALID = SQL implements the user's intent correctly
- Check actual SQL syntax, not assumptions
- Verify GROUP BY, WHERE, SELECT match request
- Accept reasonable interpretations; do not over-interpret `limit` (100,000 is default)
- When data was fetched from an external source, the table is already scoped to the fetched parameters. A `SELECT *` or unfiltered query against such a table is valid if the data overview shows it contains the requested rows.
{% endif %}
{% if memory.get('view') %}
Analyze if the generated view and responses match the user's request.
{% endif %}
{% if memory.get('chat') %}
Analyze if the assistant's response addresses the user's request given the context it had.
VALID = response uses the available context to answer; do not penalize for missing data that was never loaded or available.
{% endif %}
{%- endblock -%}

{%- block examples %}
## Examples
{% if memory.get('sql') -%}
User: "by region?"
SQL: SELECT region, SUM(sales) FROM data GROUP BY region
✓ VALID - has GROUP BY region

User: "by region?"
SQL: SELECT SUM(sales) FROM data
✗ INVALID - missing GROUP BY region

User: "top 5"
SQL: SELECT * FROM data ORDER BY revenue DESC LIMIT 5
✓ VALID - has LIMIT 5

User: "show 1000 rows"
SQL: SELECT * FROM data LIMIT 50
✗ INVALID - wrong LIMIT value

User: "total by state"
SQL: SELECT state, SUM(amount) FROM data GROUP BY state
✓ VALID - groups by state with SUM
{% endif %}
{% if memory.get('chat') -%}
User: "Summarize the survey results" → Response with field breakdowns and patterns → ✓ VALID
User: "What are the top selling products?" → Generic discussion, no rankings → ✗ INVALID
{% endif %}
{% if memory.get('view') -%}
User: "bar chart of sales by region" → bar mark with region on x, sales on y → ✓ VALID
User: "line chart over time" → bar chart with no time axis → ✗ INVALID
{% endif %}
{%- endblock -%}

{%- block context %}
## Context
{%- if memory.get('plan') %}
Response Plan:
{{ memory.plan.render_task_history()[-1] }}
{% endif -%}

Current context contains the following keys: {{ ", ".join(memory) }}

{%- if memory.get('sql') %}
SQL: `{{ memory['sql'] }}`
{%- endif -%}
{%- if memory.get('data') is not none %}
Data Overview:
{{ memory["data"] }}
{%- endif -%}
{% if memory.get('view') %}
Generated View:
```yaml
{{ memory["view"] | json_to_yaml }}
```
{%- endif -%}
{% if chat_history is defined %}
Assistant Responses:
{{ chat_history }}
{%- endif -%}
{%- if memory.get('source_actions') %}
Configured external data sources:
{%- for name, info in memory['source_actions'].items() %}
- {{ name }}: {{ info.get('description', '(no description)') }}
{%- endfor %}
{%- endif -%}
{%- endblock -%}
