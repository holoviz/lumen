{% extends 'BaseViewAgent/main.jinja2' %}

{% block instructions %}
Generate a complete, effective Mosaic (vgplot) visualization as a declarative
`mosaic-spec` in YAML that clearly communicates the data's insights.

Mosaic is closer to Observable Plot with a SQL data layer than to Vega-Lite:
marks reference DuckDB tables by name and encodings can be SQL aggregates.

## Required Top-Level Structure

A single plot MUST use the key `plot:` whose value is a list. Each mark is a
mapping with a `mark:` key naming the mark type, and its channels as siblings:

```yaml
plot:
- mark: dot                              # mark type is the VALUE of `mark:`
  data: {from: {{ memory['table'] }}}    # every mark references the table by name
  x: SomeColumn
  y: OtherColumn
  fill: SomeColumn
height: 300
```

Plot attributes (`width`, `height`, `xLabel`, `yLabel`, ...) are siblings of
`plot:`, at the top level.

## Common Mistakes

- Not `marks:` — the key is `plot:`.
- Not `- dot:` / `- regressionY:` (the mark name as the key). A mark is always
  `- mark: dot`, `- mark: regressionY`, with channels as siblings.
- Not `intervals:` or a top-level `brush:` — selections go under `params:`
  (e.g. `params: {brush: {select: intersect}}`). `intervalX`, `intervalY`, and
  `intervalXY` are plot interactors: apply one with a separate `- select:`
  entry inside the plot, using `as: $brush`.
- Not `selection: $brush` inside a mark — use `filterBy: $brush` on the mark's
  `data`, and declare the interactor as a separate `- select: intervalX` entry.
- Not `mark: table` — a linked Mosaic table is a separate `input: table`
  layout component with `from:` and `filterBy:`.

## Essential Rules

- Use only column names that appear in the dataset shown above; NEVER invent or
  assume columns (e.g. do not use `brand`, `category`, `value` unless they are
  actually present). Match their capitalization exactly.
- Reference the data on every mark with `data: {from: {{ memory['table'] }}}`.
- Do not add a top-level `data:` block declaring files or URLs — the table is
  provided automatically. Just reference it by name.
- Compose several plots with `vconcat:` / `hconcat:`; space with `vspace:` /
  `hspace:`. Focus on one primary relationship per plot.
- Prefer a simple single-mark plot unless interactivity is explicitly requested.
- Do not use scientific notation in axis ticks (`yTickFormat: s` for SI units).

## Visual Quality and Readability

- Produce a polished chart, not only a technically valid one. Use clear,
  human-readable axis labels, adequate margins, and concise titles where useful.
- Use `colorScheme: tableau10` for categorical color unless the data calls for
  a sequential or diverging palette. Avoid arbitrary harsh colors.
- Use subtle horizontal grid lines (`yGrid: true`) for quantitative charts and
  enough left/bottom margin that labels never clip.
- Add useful tooltips for marks when they help identify values. For scatter
  plots, use a visible point radius and moderate opacity so overlapping points
  remain readable.
- For a histogram split by a category, prefer one shared plot colored by that
  category with moderate opacity. Use `fx`/`fy` facets only when the user asks
  for separate panels or when overlapping distributions would be misleading.
- Avoid layouts with tiny labels, overly narrow bars, excessive empty space, or
  more panels than the available area can display clearly.

## Marks

Use vgplot mark names, not Vega-Lite names:
- `lineY` — line chart (x ordered, y value)
- `barY` / `barX` — vertical / horizontal bars for categorical data
- `rectY` — histogram bars (pair with `x: {bin: col}`)
- `dot` — scatter plot
- `areaY` — filled area
- `text` — labels/annotations
- `errorbarX` / `regressionY` — statistical marks

## Encodings

Channels are `x`, `y`, `fill`, `stroke`, `r`, `fx`, `fy`. Values are either a
bare column name or a SQL transform object:
- Aggregates: `{sum: col}`, `{avg: col}`, `{count: }`, `{max: col}`, `{min: col}`
- Binning: `{bin: col}` (for histograms/heatmaps)
- Static appearance: `fill: steelblue`, `stroke: black`, `r: 3`
- Sorting/limiting a categorical axis: `sort: {y: -x, limit: 10}`

## Plot Attributes

Set these as keys on the plot (siblings of `plot:`), not inside marks:
`xLabel`, `yLabel`, `xDomain`, `yDomain`, `width`, `height`, `marginLeft`,
`colorScheme`, `yTickFormat`.

## Interactivity (Mosaic's Strength, Use When It Adds Value)

Declare a selection mode in `params:` and reference it with `$name`. The
selection mode is one of `intersect`, `crossfilter`, `single`, or `union`.
`intervalX`, `intervalY`, and `intervalXY` are interactors used within a plot.
`value` is a separate non-selection parameter mode.

- For one brush that filters other views, use `params: {brush: {select: intersect}}`.
  Put `- select: intervalXY` / `as: $brush` in the source plot. Put
  `data: {from: {{ memory['table'] }}, filterBy: $brush}` on every target mark.
- Use `crossfilter` only when two or more views should each contribute a brush
  and filter one another.
- Inputs are layout components: `input: menu`, `input: slider`, and
  `input: table`. A linked table uses `from: {{ memory['table'] }}` and
  `filterBy: $brush`, not `mark: table`.
- Every reactive input binding must include the `$` reference prefix:
  `as: $filter`, never `as: filter`. Likewise, use `filterBy: $filter`, never
  `filterBy: filter`. Omitting `$` passes a plain string to Mosaic and causes a
  browser error such as `addEventListener is not a function`.
- For a categorical dropdown that filters data, declare a selection such as
  `params: {filter: {select: intersect}}`, then use an `input: menu` with
  `from: <table>`, `column: <categorical column>`, and `as: $filter`. Apply
  `filterBy: $filter` to every target mark or table. Prefer deriving options
  from `from` and `column` instead of manually inventing `options`.
- A plain slider changes only a numeric parameter; it does **not** filter rows.
  To make a slider filter a plot or table, declare a selection in `params:`,
  set the slider's `select: interval`, and use that selection with `filterBy:`.
  For example, `params: {range: {select: intersect}}`, then an
  `input: slider` with `select: interval`, `as: $range`, `from: <table>`, and
  `column: <numeric column>`. Target views must use
  `data: {from: <table>, filterBy: $range}`. A Mosaic interval slider filters
  from the column minimum to the selected value; do not describe it as a
  two-handle range slider.

Only add interactivity when the request calls for exploration; a simple chart
should stay simple.
{% endblock %}

{% block examples %}
## Examples (illustrative — the column names below are placeholders, not real data)
{% if doc_examples is defined and doc_examples %}
{% for example in doc_examples %}
{{ example }}
{% endfor %}
{% endif %}

Simple line chart:
```yaml
plot:
- mark: lineY
  data: {from: {{ memory['table'] }}}
  x: date
  y: close
width: 680
height: 240
```

Aggregated, sorted, limited bar chart:
```yaml
plot:
- mark: barX
  data: {from: {{ memory['table'] }}}
  x: {sum: gold}
  y: nationality
  fill: steelblue
  sort: {y: -x, limit: 10}
xLabel: Gold Medals
yLabel: Nationality
```

Histogram (binned + counted):
```yaml
plot:
- mark: rectY
  data: {from: {{ memory['table'] }}}
  x: {bin: delay}
  y: {count: }
  fill: steelblue
  insetLeft: 0.5
  insetRight: 0.5
xLabel: Arrival Delay (min)
yTickFormat: s
height: 220
```

Scatter plot:
```yaml
plot:
- mark: dot
  data: {from: {{ memory['table'] }}}
  x: weight
  y: height
  fill: sex
  r: 3
  fillOpacity: 0.5
```

Scatter plot with a regression line and interactive selection (note: the
regression is a second `- mark:` entry, and the selection is a separate
`- select:` entry — never a `selection:` field inside a mark, never `marks:`):
```yaml
params:
  query: {select: intersect}
plot:
- mark: dot
  data: {from: {{ memory['table'] }}}
  x: weight
  y: height
  fill: sex
  r: 2
  opacity: 0.1
- mark: regressionY
  data: {from: {{ memory['table'] }}, filterBy: $query}
  x: weight
  y: height
  stroke: sex
- select: intervalXY
  as: $query
```

Linked scatter, histogram, and table. Drag on the scatter plot: the histogram
and table update to only the selected rows.
```yaml
params:
  brush: {select: intersect}
hconcat:
- plot:
  - mark: dot
    data: {from: {{ memory['table'] }}}
    x: x_value
    y: y_value
    fill: category
    r: 3
    opacity: 0.7
  - select: intervalXY
    as: $brush
  xLabel: X value
  yLabel: Y value
  width: 480
  height: 320
- vconcat:
  - plot:
    - mark: rectY
      data: {from: {{ memory['table'] }}, filterBy: $brush}
      x: {bin: measure}
      y: {count: }
      fill: steelblue
      insetLeft: 1
      insetRight: 1
    xLabel: Measure
    yLabel: Selected rows
    width: 400
    height: 160
  - input: table
    from: {{ memory['table'] }}
    filterBy: $brush
    height: 180
    width: 400
```

Linked slider. Moving the slider filters both the histogram and table to rows
whose numeric value is between the column minimum and the chosen maximum.
```yaml
params:
  range: {select: intersect}
vconcat:
- input: slider
  label: Maximum value
  select: interval
  as: $range
  from: {{ memory['table'] }}
  column: measure
  step: 1
- hconcat:
  - plot:
    - mark: rectY
      data: {from: {{ memory['table'] }}, filterBy: $range}
      x: {bin: measure}
      y: {count: }
      fill: steelblue
    xLabel: Measure
    yLabel: Selected rows
  - input: table
    from: {{ memory['table'] }}
    filterBy: $range
    height: 240
```

Linked categorical dropdown. Choosing a category filters the plot and table.
```yaml
params:
  category_filter: {select: intersect}
vconcat:
- input: menu
  label: Category
  from: {{ memory['table'] }}
  column: category
  as: $category_filter
- hconcat:
  - plot:
    - mark: dot
      data: {from: {{ memory['table'] }}, filterBy: $category_filter}
      x: x_value
      y: y_value
      fill: category
  - input: table
    from: {{ memory['table'] }}
    filterBy: $category_filter
    height: 240
```
{% endblock %}
